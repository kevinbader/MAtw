/**
 * Copyright 2014 Kevin Bader.
 * Distributed under the GNU GPL v3. For full terms see the file /LICENSE.
 */
#include <gtest/gtest.h>
#include <common.h>
#include <localsearch.h>

using namespace d2;

TEST(localsearch, LS1) {
    // TODO make some USEFUL tests
    //auto ordering = ordering_t{0, 1};
    //auto g = Graph{2};
    //g.add_edge(0, 1);
    //std::default_random_engine randgen;
    //LS::LS1(&ordering, g, randgen, 10, std::numeric_limits<clock_t>::max());
    //LS::testme();
}
